# Đưa video sang Cloudflare R2

Mục tiêu: video không tính vào 100 GB/tháng của Vercel Hobby nữa. R2 **không thu phí
egress**, free 10 GB lưu trữ + 10 triệu lượt đọc/tháng.

Sau khi xong, Vercel chỉ còn phục vụ ~64 KB/lượt:

| | Hiện tại (video trên Vercel) | Sau khi sang R2 |
|---|---|---|
| 1000 lượt/ngày | 65 GB/tháng (65% hạn mức) | **1,9 GB/tháng** |

Lợi ích thứ hai, lâu dài hơn: **video không nằm trong git nữa**. Mỗi lần quay lại hay
nén lại một video là git giữ thêm một bản vĩnh viễn — 20 tượng × vài lần sửa là repo
phình cả trăm MB, không gỡ được nếu không viết lại lịch sử.

---

## ⚠️ Điều kiện bắt buộc: phải dời nameserver về Cloudflare

Đây là phần đắt nhất, phải biết trước khi bắt đầu.

- URL công khai `*.r2.dev` **không dùng cho production được** — tài liệu Cloudflare ghi
  rõ nó bị rate-limit và chỉ dành cho phát triển. Dùng nó là tự đặt bom hẹn giờ.
- Muốn có custom domain cho bucket thì domain **phải là zone trong cùng account
  Cloudflare**.
- Hai đường vòng để khỏi dời nameserver đều không có ở gói free:
  - partial (CNAME) setup → cần **Business**
  - subdomain zone setup → cần **Enterprise**

⇒ Phải chuyển `trustpage.info` từ Mat Bao sang nameserver Cloudflare.

Tin tốt: bộ bản ghi DNS hiện tại **chỉ có 5 cái**, đã kiểm kê đầy đủ ở dưới. Việc này
làm cẩn thận thì an toàn.

### Bảng DNS phải có đủ trong Cloudflare trước khi đổi nameserver

Kiểm kê ngày 18/08/2026. Cloudflare khi thêm domain sẽ tự quét và import phần lớn,
nhưng **phải đối chiếu tay với bảng này**, thiếu MX hoặc DKIM là hỏng email.

| Type | Name | Value | Proxy | Ghi chú |
|---|---|---|---|---|
| A | `@` | `76.76.21.21` | **DNS only** (mây xám) | Vercel |
| CNAME | `www` | `cname.vercel-dns.com` | **DNS only** | Vercel |
| MX | `@` | `mx.zoho.com` (priority 10) | — | **Email. Thiếu là mất mail.** |
| TXT | `@` | `zoho-verification=zb13371927.zmverify.zoho.com` | — | xác minh Zoho |
| TXT | `zmail._domainkey` | `v=DKIM1; k=rsa; p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQDEbnHVFknFx5139KlkJMCHecpdUKn+B+IjWBrgmjbZ3BCBTPqtRjl9R29Phoh8coZMNzAIt/4lnFU4BcW+1NBacFv7zY9tKYMlPeqxvH02SUWrN8FxJTnIDMUcFBa7cZ1iYLDCZLHU0K3Oog6O7ozbCF8Nmvk4JZUgdhsSSYLJzwIDAQAB` | — | DKIM Zoho |

**Bản ghi Vercel phải để DNS only (mây xám), đừng bật proxy.** Bật proxy là chồng hai
CDN lên nhau, dễ sinh lỗi chứng chỉ và cache khó lần.

Lấy lại bảng này bất cứ lúc nào:

```bash
dig +short A trustpage.info; dig +short CNAME www.trustpage.info
dig +short MX trustpage.info; dig +short TXT trustpage.info
dig +short TXT zmail._domainkey.trustpage.info
```

> Nhân tiện: domain **chưa có bản ghi SPF**. Không liên quan tới R2, nhưng đang đụng
> DNS thì thêm luôn cho email đỡ vào spam: TXT `@` = `v=spf1 include:zoho.com ~all`.
> Kiểm với Zoho cho chắc trước khi thêm.

### Các bước dời nameserver

1. Cloudflare → **Add a domain** → `trustpage.info` → chọn gói **Free**.
2. Nó quét DNS tự động. **Đối chiếu với bảng trên, thiếu cái nào thêm tay.**
3. Chỉ khi đủ 5 bản ghi mới sang registrar (**Mat Bao**) đổi nameserver thành 2 địa chỉ
   Cloudflare đưa.
4. Chờ lan (thường vài phút tới vài giờ). Trong lúc chờ, web và email vẫn chạy bằng
   DNS cũ — không có downtime nếu bản ghi đã khớp.
5. Sau khi Cloudflare báo Active, **test email**: gửi thử một thư vào hộp thư của
   domain và gửi một thư đi.

---

## Sau khi nameserver đã ở Cloudflare

### 1. Tạo bucket

Cloudflare → **R2** → **Create bucket** → tên `buddha-video` → Location: Automatic.

### 2. Upload video

```bash
npx wrangler login        # làm 1 lần
./upload-r2.sh            # đẩy hết web/videos/*.mp4 lên bucket
```

Tên file chính là id trong `content.json` (`dia-tang.mp4`), nên URL cuối cùng khớp
luôn, không phải ánh xạ gì.

### 3. Gắn custom domain cho bucket

Bucket → **Settings** → **Custom Domains** → **Connect Domain** → `video.trustpage.info`.

Vì zone đã nằm trong Cloudflare nên nó tự tạo bản ghi DNS, không phải làm tay.

### 4. Chuyển app sang dùng R2

Sửa đúng **một dòng** trong `web/content.json`:

```json
"videoBase": "https://video.trustpage.info/"
```

Xong. Không đụng một dòng code nào — `videoBase` được ghép với tên file lúc render.
Muốn quay về phục vụ từ Vercel thì đổi lại thành `"videos/"`.

Kiểm tra trước khi push:

```bash
curl -sI https://video.trustpage.info/dia-tang.mp4 | head -3
# mong đợi: HTTP/2 200 và content-type: video/mp4
```

### 5. Gỡ video khỏi git cho repo khỏi phình

Chỉ làm **sau khi** R2 đã chạy và đã test xong:

```bash
git rm --cached web/videos/*.mp4
printf 'web/videos/*.mp4\n' >> .gitignore
git commit -m "Video chuyển sang R2, không track trong git nữa"
```

File vẫn nằm trên máy để `dev.sh` chạy local được. Lịch sử git giữ lại mấy bản cũ
(khoảng 4 MB) — không đáng để viết lại lịch sử.

---

## Không cần cấu hình CORS

Thẻ `<video src="...">` không có thuộc tính `crossorigin` nên trình duyệt tải kiểu
no-cors, R2 custom domain trả bình thường. Chỉ phải đụng tới CORS nếu sau này tải video
bằng `fetch()`, mà app này không làm thế.

## Ghi chú: dời nameserver rồi thì còn một lựa chọn nữa

Khi `trustpage.info` đã ở Cloudflare, ông có thể bỏ luôn Vercel và đưa cả app sang
**Cloudflare Pages** — băng thông không giới hạn, thế là không cần R2 nữa, một nền tảng
thay vì hai.

Tôi vẫn nghiêng về R2 như trên, vì lý do còn lại sau khi băng thông đã hết là chuyện
**video không nằm trong git**. Đó là thứ Pages không giải quyết được. Nhưng nếu sau này
thấy hai nền tảng phiền quá thì đường đó vẫn mở.
